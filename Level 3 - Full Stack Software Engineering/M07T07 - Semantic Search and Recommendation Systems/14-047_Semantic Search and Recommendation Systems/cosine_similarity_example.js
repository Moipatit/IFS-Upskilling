function cosineSimilarity(vectorA, vectorB) { 
    // Calculate the dot product.
    // Calculate the magnitude of vectorA.
    // Calculate the magnitude of vectorB.
    // Return the cosine similarity score.
}

function searchCourses(query) {
  // Find the matching query embedding. 
  // Compare it with each course embedding.
  // Sort the results by similarity score. 
  // Return the top three results.
}

function recommendCourses(selectedCourse) {
// Compare the selected course with all other courses. 
// Exclude the selected course. 
// Sort by similarity score. 
  // Return the top two recommendations. 
}